apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: mta-fbc-e2e-pipeline
spec:
  description: |
    Deploy MTA operator from FBC catalog to ephemeral Hypershift cluster and run E2E tests.
    Uses imageContentSources to configure registry mirroring.

  params:
    - name: SNAPSHOT
      description: Application Snapshot JSON
      type: string

  results:
    - name: CLUSTER_NAME
      description: Name of the provisioned cluster
      value: $(tasks.provision-cluster.results.clusterName)
    - name: DEPLOYMENT_STATUS
      description: Operator deployment status
      value: $(tasks.deploy-operator.results.deploymentStatus)
    - name: TEST_OUTPUT
      description: Test execution result
      value: $(tasks.run-e2e-tests.results.testStatus)

  tasks:
    - name: parse-metadata
      params:
        - name: SNAPSHOT
          value: $(params.SNAPSHOT)
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/konflux-ci/integration-examples
          - name: revision
            value: a1a70b0a1cfc96f5216d472fbd60f6b42780b3e5
          - name: pathInRepo
            value: tasks/test_metadata.yaml

    - name: provision-eaas-space
      runAfter:
        - parse-metadata
      params:
        - name: ownerName
          value: $(context.pipelineRun.name)
        - name: ownerUid
          value: $(context.pipelineRun.uid)
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/konflux-ci/build-definitions.git
          - name: revision
            value: 9134d57bf28a5566ffbf9710c1c534ade1c69cd2
          - name: pathInRepo
            value: task/eaas-provision-space/0.1/eaas-provision-space.yaml

    - name: provision-cluster
      runAfter:
        - provision-eaas-space
      params:
        - name: eaasSpaceSecretRef
          value: $(tasks.provision-eaas-space.results.secretRef)
      taskSpec:
        params:
          - name: eaasSpaceSecretRef
        results:
          - name: clusterName
        steps:
          - name: create-cluster
            ref:
              resolver: git
              params:
                - name: url
                  value: https://github.com/konflux-ci/build-definitions.git
                - name: revision
                  value: 9134d57bf28a5566ffbf9710c1c534ade1c69cd2
                - name: pathInRepo
                  value: stepactions/eaas-create-ephemeral-cluster-hypershift-aws/0.1/eaas-create-ephemeral-cluster-hypershift-aws.yaml
            params:
              - name: eaasSpaceSecretRef
                value: $(params.eaasSpaceSecretRef)
              - name: version
                value: "4.17.0"
              - name: instanceType
                value: "m5.xlarge"
              - name: imageContentSources
                value: |
                  - source: registry.redhat.io
                    mirrors:
                    - registry.stage.redhat.io
          - name: capture-result
            image: registry.access.redhat.com/ubi9/ubi-minimal@sha256:12db9874bd753eb98b1ab3d840e75de5d6842ac0604fbd68c012adefe97140be
            env:
              - name: CLUSTER_NAME
                value: $(steps.create-cluster.results.clusterName)
            script: |
              #!/bin/bash
              echo -n "${CLUSTER_NAME}" > $(results.clusterName.path)

    - name: deploy-operator
      runAfter:
        - provision-cluster
      params:
        - name: eaasSpaceSecretRef
          value: $(tasks.provision-eaas-space.results.secretRef)
        - name: clusterName
          value: $(tasks.provision-cluster.results.clusterName)
        - name: fbcImage
          value: $(tasks.parse-metadata.results.component-container-image)
        - name: packageName
          value: "mta-operator"
        - name: channelName
          value: "stable-v8.1"
        - name: operatorNamespace
          value: "openshift-mta"
      taskSpec:
        params:
          - name: eaasSpaceSecretRef
          - name: clusterName
          - name: fbcImage
          - name: packageName
          - name: channelName
          - name: operatorNamespace
        results:
          - name: deploymentStatus
            description: Deployment status
          - name: csvName
            description: Installed ClusterServiceVersion name
          - name: uiRoute
            description: MTA UI route hostname
          - name: keycloakPassword
            description: Keycloak admin password
        volumes:
          - name: credentials
            emptyDir: {}
        steps:
          - name: get-kubeconfig
            ref:
              resolver: git
              params:
                - name: url
                  value: https://github.com/konflux-ci/build-definitions.git
                - name: revision
                  value: 9134d57bf28a5566ffbf9710c1c534ade1c69cd2
                - name: pathInRepo
                  value: stepactions/eaas-get-ephemeral-cluster-credentials/0.1/eaas-get-ephemeral-cluster-credentials.yaml
            params:
              - name: eaasSpaceSecretRef
                value: $(params.eaasSpaceSecretRef)
              - name: clusterName
                value: $(params.clusterName)
              - name: credentials
                value: credentials

          - name: install-operator
            image: quay.io/openshift/origin-cli@sha256:c9b8b7c808abfb959dd4b0545407fcd8436d6b2ebe9636053e90d3293a81b5a9
            volumeMounts:
              - name: credentials
                mountPath: /credentials
            env:
              - name: KUBECONFIG
                value: /credentials/$(steps.get-kubeconfig.results.kubeconfig)
            script: |
              #!/bin/bash
              set -euo pipefail
              trap 'echo "FAILED" > $(results.deploymentStatus.path)' ERR

              FBC_IMAGE="$(params.fbcImage)"
              PACKAGE_NAME="$(params.packageName)"
              CHANNEL_NAME="$(params.channelName)"
              NAMESPACE="$(params.operatorNamespace)"

              oc create namespace "$NAMESPACE" --dry-run=client -o yaml | oc apply -f -

              cat <<EOF | oc apply -f -
              apiVersion: operators.coreos.com/v1alpha1
              kind: CatalogSource
              metadata:
                name: mta-konflux-catalog
                namespace: openshift-marketplace
              spec:
                sourceType: grpc
                image: $FBC_IMAGE
                displayName: MTA Konflux Catalog
                publisher: Red Hat
                grpcPodConfig:
                  securityContextConfig: restricted
                  extractContent:
                    catalogDir: /configs
                    cacheDir: /tmp/cache
                updateStrategy:
                  registryPoll:
                    interval: 10m
              EOF

              for i in {1..60}; do
                STATE=$(oc get catalogsource mta-konflux-catalog -n openshift-marketplace -o jsonpath='{.status.connectionState.lastObservedState}' 2>/dev/null || echo "")
                if [ "$STATE" = "READY" ]; then
                  echo "✅ CatalogSource READY"
                  break
                fi
                if [ $i -eq 60 ]; then
                  echo "FAILED" > $(results.deploymentStatus.path)
                  exit 1
                fi
                sleep 5
              done

              cat <<EOF | oc apply -f -
              apiVersion: operators.coreos.com/v1
              kind: OperatorGroup
              metadata:
                name: mta-operator-group
                namespace: $NAMESPACE
              spec:
                targetNamespaces:
                  - $NAMESPACE
              EOF

              cat <<EOF | oc apply -f -
              apiVersion: operators.coreos.com/v1alpha1
              kind: Subscription
              metadata:
                name: mta-operator
                namespace: $NAMESPACE
              spec:
                channel: $CHANNEL_NAME
                installPlanApproval: Automatic
                name: $PACKAGE_NAME
                source: mta-konflux-catalog
                sourceNamespace: openshift-marketplace
              EOF

              echo "Waiting for operator installation (15 minute timeout)..."
              sleep 10

              CSV=""
              PHASE=""
              for i in {1..180}; do
                CSV=$(oc get subscription mta-operator -n "$NAMESPACE" -o jsonpath='{.status.installedCSV}' 2>/dev/null || echo "")
                if [ -n "$CSV" ]; then
                  PHASE=$(oc get csv "$CSV" -n "$NAMESPACE" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
                  if [ "$PHASE" = "Succeeded" ]; then
                    echo "✅ Operator installed: $CSV"
                    echo "$CSV" > $(results.csvName.path)
                    break
                  fi
                fi

                if [ $((i % 12)) -eq 0 ]; then
                  echo "Waiting for CSV... ($i/180)"
                fi
                sleep 5
              done

              if [ -z "$CSV" ] || [ "$PHASE" != "Succeeded" ]; then
                echo "❌ Operator installation failed or timed out after 15 minutes"
                echo "CSV: $CSV, Phase: ${PHASE:-not set}"
                echo ""
                echo "Diagnostics:"
                echo "Subscription status:"
                oc get subscription mta-operator -n "$NAMESPACE" -o yaml 2>/dev/null | grep -A 20 "status:" || echo "  No subscription status"
                echo ""
                echo "InstallPlans:"
                oc get installplan -n "$NAMESPACE" 2>/dev/null || echo "  No InstallPlans found"
                echo ""
                echo "Events:"
                oc get events -n "$NAMESPACE" --sort-by='.lastTimestamp' 2>/dev/null | tail -10 || echo "  No events"
                echo "FAILED" > $(results.deploymentStatus.path)
                exit 1
              fi

              oc wait --for=condition=available --timeout=300s deployment/mta-operator -n "$NAMESPACE"

              cat <<EOF | oc apply -f -
              apiVersion: tackle.konveyor.io/v1alpha1
              kind: Tackle
              metadata:
                name: mta
                namespace: $NAMESPACE
              spec:
                provider_c_sharp_container_requests_memory: 512Mi
                provider_c_sharp_container_limits_memory: 512Mi
                provider_c_sharp_container_requests_cpu: 500m
                provider_c_sharp_container_limits_cpu: 500m
                language_discovery_container_limits_cpu: 500m
                language_discovery_container_requests_cpu: 500m
                language_discovery_container_limits_memory: 512Mi
                language_discovery_container_requests_memory: 512Mi
                analyzer_container_limits_cpu: 500m
                analyzer_container_requests_cpu: 500m
                analyzer_container_limits_memory: 512Mi
                analyzer_container_requests_memory: 512Mi
                provider_generic_container_limits_cpu: 500m
                provider_generic_container_requests_cpu: 500m
                provider_generic_container_limits_memory: 512Mi
                provider_generic_container_requests_memory: 512Mi
                provider_nodejs_container_limits_cpu: 500m
                provider_nodejs_container_requests_cpu: 500m
                provider_nodejs_container_limits_memory: 512Mi
                provider_nodejs_container_requests_memory: 512Mi
                hub_bucket_volume_size: "25Gi"
                cache_data_volume_size: "25Gi"
              EOF

              echo "Waiting for MTA UI deployment to be created..."
              for i in {1..120}; do
                if oc get deployment mta-ui -n "$NAMESPACE" &>/dev/null; then
                  echo "✅ MTA UI deployment exists"
                  break
                fi
                if [ $i -eq 120 ]; then
                  echo "ERROR: MTA UI deployment not created after 10 minutes"
                  echo "FAILED" > $(results.deploymentStatus.path)
                  exit 1
                fi
                sleep 5
              done

              echo "Waiting for MTA UI deployment to roll out..."
              oc rollout status deployment/mta-ui -n "$NAMESPACE" --timeout=900s

              UI_ROUTE=$(oc get route mta -n "$NAMESPACE" -o jsonpath='{.spec.host}' 2>/dev/null || echo "")
              if [ -z "$UI_ROUTE" ]; then
                echo "ERROR: Could not get MTA route"
                echo "FAILED" > $(results.deploymentStatus.path)
                exit 1
              fi

              echo "✅ MTA UI: https://$UI_ROUTE"
              echo -n "$UI_ROUTE" > $(results.uiRoute.path)

              echo "Getting Keycloak admin password..."
              KEYCLOAK_SECRET="mta-keycloak-rhbk"
              for i in {1..60}; do
                if oc get secret "$KEYCLOAK_SECRET" -n "$NAMESPACE" &>/dev/null; then
                  KEYCLOAK_PASS=$(oc get secret "$KEYCLOAK_SECRET" -n "$NAMESPACE" -o jsonpath='{.data.password}' | base64 -d)
                  echo "✅ Keycloak password retrieved"
                  echo -n "$KEYCLOAK_PASS" > $(results.keycloakPassword.path)
                  break
                fi
                if [ $i -eq 60 ]; then
                  echo "ERROR: Keycloak secret not created after 5 minutes"
                  echo "FAILED" > $(results.deploymentStatus.path)
                  exit 1
                fi
                sleep 5
              done

              echo "SUCCESS" > $(results.deploymentStatus.path)

    - name: run-e2e-tests
      runAfter:
        - deploy-operator
      params:
        - name: uiRoute
          value: $(tasks.deploy-operator.results.uiRoute)
        - name: keycloakPassword
          value: $(tasks.deploy-operator.results.keycloakPassword)
      taskSpec:
        params:
          - name: uiRoute
          - name: keycloakPassword
        results:
          - name: testStatus
            description: Test execution status (PASSED or FAILED)
        steps:
          - name: run-tests
            image: cypress/included:15.7.0
            env:
              - name: CYPRESS_CACHE_FOLDER
                value: /tmp/.cache/Cypress
              - name: npm_config_cache
                value: /tmp/.cache/npm
            script: |
              #!/bin/bash
              set -euo pipefail
              trap 'echo "FAILED" > $(results.testStatus.path)' ERR

              UI_ROUTE="$(params.uiRoute)"
              KEYCLOAK_PASS="$(params.keycloakPassword)"
              BASE_URL="https://${UI_ROUTE}"

              echo "MTA UI: $BASE_URL"

              echo "Cloning mta-tackle2-ui repository..."
              cd /tmp
              git clone --depth 1 --branch main https://github.com/migtools/mta-tackle2-ui.git
              cd mta-tackle2-ui

              echo "Installing dependencies..."
              npm clean-install --ignore-scripts --no-audit
              npx cypress install

              echo "Running login test only..."
              cd cypress
              cp .env.example .env

              export CYPRESS_BASE_URL="$BASE_URL"
              export CYPRESS_KEYCLOAK_ADMIN_PASSWORD="$KEYCLOAK_PASS"

              if npx dotenvx run -- npx cypress run \
                --spec "e2e/tests/login.test.ts" \
                --config-file cypress.config.ts; then
                echo "PASSED" > $(results.testStatus.path)
              else
                echo "FAILED" > $(results.testStatus.path)
                exit 1
              fi
